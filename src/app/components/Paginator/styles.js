import styled from 'styled-components'
import {Grid} from '@mui/material'

export const StyledPaginationWrapper = styled(Grid).attrs(() => ({
  container: true,
  xs: 12,
}))`
  justify-content: center;
  padding: 20px;
`
